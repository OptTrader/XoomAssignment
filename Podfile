platform :ios, '9.0'

target 'XoomAssignment' do
    use_frameworks!
    pod 'BRYXBanner'
    pod 'CDAlertView'

    target 'XoomAssignmentTests' do
        inherit! :search_paths
        pod 'Quick'
        pod 'Nimble'
    end

end
